spring:
  application:
    name: gateway # 服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.33.164:8848 # nacos地址
      config:
        file-extension: yaml #文件名后缀
        shared-configs:
          - data-id: shared-log.yaml # 共享日志配置